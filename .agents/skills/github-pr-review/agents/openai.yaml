interface:
  display_name: "GitHub PR Review"
  short_description: "Review agent pull requests independently"
  default_prompt: "Use $github-pr-review to independently review the selected pull request."
